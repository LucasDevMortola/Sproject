'use strict';

/**
 *  reconhecimento controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reconhecimento.reconhecimento');
